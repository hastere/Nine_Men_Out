<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ninemenout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".AcceptFriendBet"></activity>
        <activity android:name=".FriendBetsViewerActivity" />
        <activity android:name=".ViewRequestsActivity" />
        <activity android:name=".AddFriendsActivity" />
        <activity
            android:name=".placeBetActivity"
            android:label="@string/title_activity_place_bet"
            android:theme="@style/NoActionBar" />
        <activity android:name=".SuggestedBetsActivity" />
        <activity
            android:name=".BetsViewerActivity"
            android:label="@string/title_activity_bets_viewer"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".SearchBetsBufferActivity"
            android:label="@string/title_activity_search_bets_buffer"
            android:theme="@style/NoActionBar" />
        <activity android:name=".SuggestedBetsActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".SearchBetsActivity" />
        <activity android:name=".PersonalBetActivity" />
        <activity android:name=".browseGames" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>